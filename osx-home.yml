---
- hosts: localhost
  connection: local
  vars:
    user: darrenbuse
  sudo: no
  tasks:
      - name: set hostname
        shell: 'scutil --set HostName "heneretta" && scutil --set LocalHostName "heneretta"'
        sudo: True
  roles:
    - db.osx-base
    - db.known_hosts
    - dotfiles
    - db.osx-misc

